# Development and Testing Dependencies
# Install with: pip install -r requirements-dev.txt

# Include all production dependencies
-r requirements.txt

# Test framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Code quality
pylint>=3.0.0
flake8>=7.0.0

# Optional hardware dependencies (may fail to build in CI)
# Uncomment for local hardware testing:
# ELM327-emulator>=1.1.1

# Note: Hardware tests are marked with @pytest.mark.hardware
# Skip in CI with: pytest -m "not hardware"
