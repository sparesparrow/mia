[pytest]
# Pytest Configuration for MIA Project

# Minimum pytest version
minversion = 8.0

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Asyncio mode
asyncio_mode = auto

# Test markers
markers =
    hardware: tests that require physical hardware (deselect with '-m "not hardware"')
    slow: slow-running tests (deselect with '-m "not slow"')
    integration: integration tests
    unit: unit tests
    automotive: automotive-specific tests
    android: Android app tests (requires Android SDK)
    meta_harness: Meta-Harness optimization loop tests

# Output options
addopts =
    -ra
    --strict-markers
    --tb=short
    --cov
    --cov-report=term-missing
    --cov-report=html

# Coverage options
[coverage:run]
source = rpi,modules,services,agents,Mia,orchestration
omit =
    */tests/*
    */test_*
    */__pycache__/*
    */site-packages/*
    */firmware/*
    */apps/android/*

[coverage:report]
precision = 2
show_missing = True
skip_covered = False
