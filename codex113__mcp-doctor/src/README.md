# MCP Doctor

Local MCP configuration diagnostics for workspaces and desktop client configs.

Run from this directory with:

```bash
PYTHONPATH=src python3 -m mcp_doctor --self-test --format text
```
