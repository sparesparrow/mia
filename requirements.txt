# AI-SERVIS Universal Requirements
# Core automotive AI voice control system dependencies

# Core web framework and async
asyncio-mqtt==0.16.1
aiohttp==3.9.1
websockets==12.0
pydantic==2.5.2
python-multipart==0.0.6
uvicorn==0.25.0
fastapi==0.108.0

# Database and storage
psycopg2-binary==2.9.9
redis==5.0.1
sqlalchemy==2.0.25
alembic==1.13.1

# Security and authentication
python-dotenv==1.0.0
PyJWT==2.8.0
cryptography==43.0.1

# HTTP client and utilities
tenacity==8.2.3
httpx==0.26.0

# Enhanced automotive dependencies
docker==6.1.3
psutil==5.9.6
numpy==1.24.4
matplotlib==3.8.2
seaborn==0.13.0

# Voice processing and audio
librosa==0.10.1
soundfile==0.12.1
torch==2.2.0
torchaudio==2.2.0

# Monitoring and observability
prometheus-client==0.19.0

# Performance testing
locust==2.17.0

# Security scanning
bandit==1.7.5
# Note: safety 2.3.5 conflicts with black 24.3.0 (packaging version conflict)
# Using pip-tools or allowing pip to resolve is recommended for complex dependencies
# For now, we'll let pip resolve the conflict or skip safety in CI if needed
safety>=2.3.5

# Development tools (security fixes)
# Note: black 24.3.0 requires packaging>=22.0, but safety 2.3.5 requires packaging<22.0
# Using black 23.x to be compatible with safety
black==23.12.1
isort==5.12.0
mypy==1.7.1

# JSON processing (security fix)
orjson==3.9.15

# YAML processing (fix for CI build issues - pin to version with wheels)
PyYAML==6.0.1
